    1  ip link show
    2  nmcli device status
    3  sudo nmcli connection add type ethernet ifname enp0s3 con-name host-only   ipv4.addresses 192.168.1.100/24   ipv4.gateway 192.168.1.1   ipv4.dns 8.8.8.8   ipv4.method manual   ipv6.method ignore
    4  sudo nmcli connection up host-only
    5  sudo hostnamectl set-hostname mephi-2026.domain.local
    6  hostnamectl
    7  ping -c 4 8.8.8.8 > /tmp/network_check.txt
    8  ping -c 4 192.168.1.1 >> /tmp/network_check.txt
    9  cat /tmp/network_check.txt
   10  sudo nmcli connection modify host-only ipv4.never-default yes
   11  sudo nmcli connection modify "Проводное подключение 1" ipv4.method auto ipv4.never-default no
   12  sudo nmcli connection up host-only
   13  sudo nmcli connection up "Проводное подключение 1"
   14  ip route
   15  ping -c 2 8.8.8.8
   16  ping -c 2 192.168.1.1
   17  sudo dnf install -y nginx tcpdump libcap-ng-utils
   18  sudo dnf download --destdir=/tmp tcpdump
   19  sudo rpm -ivh /tmp/tcpdump*.rpm
   20  nginx -v
   21  tcpdump --version
   22  rpm -q libcap-ng-utils
   23  ls /tmp/tcpdump*.rpm
   24  lsblk
   25  sudo fdisk /dev/sdb
   26  sudo mkfs.ext4 -L MEPHI_DATA /dev/sdb1
   27  sudo mkdir -p /data/mephi-web
   28  echo 'LABEL=MEPHI_DATA /data/mephi-web ext4 defaults 0 2' | sudo tee -a /etc/fstab
   29  sudo systemctl daemon-reload
   30  sudo mount -a
   31  df -h /data/mephi-web
   32  sudo systemctl enable --now nginx
   33  sudo systemctl status nginx
   34  journalctl -u nginx --since "5 minutes ago" > /tmp/nginx_recent_logs.txt
   35  sudo groupadd mephi-devs
   36  sudo useradd -m -G mephi-devs mephi-admin
   37  echo 'mephi-admin:P@ssw0rd2026' | sudo chpasswd
   38  sudo chown mephi-admin:mephi-devs /data/mephi-web
   39  sudo chmod 2775 /data/mephi-web
   40  stat /data/mephi-web
   41  sudo setenforce 1
   42  getenforce
   43  sudo dnf install -y policycoreutils-python-utils
   44  sudo semanage fcontext -a -t httpd_sys_content_t "/data/mephi-web(/.*)?"
   45  sudo restorecon -Rv /data/mephi-web
   46  ls -Zd /data/mephi-web
   47  sudo tee /etc/nginx/conf.d/mephi-web.conf << 'EOF'
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    root /data/mephi-web;
    index index.html;
    location / {
        try_files $uri $uri/ =404;
    }
}
EOF

   48  sudo nginx -t
   49  sudo systemctl reload nginx
   50  sudo setcap -r /usr/sbin/tcpdump
   51  sudo setcap cap_net_raw,cap_net_admin+ep /usr/sbin/tcpdump
   52  getcap /usr/sbin/tcpdump
   53  sudo -u mephi-admin tcpdump --help
   54  echo 'root' | sudo tee /etc/ssh/denied_users
   55  sudo chmod 600 /etc/ssh/denied_users
   56  sudo tee -a /etc/pam.d/system-auth << 'EOF'
auth required pam_listfile.so item=user sense=deny file=/etc/ssh/denied_users onerr=succeed
EOF

   57  sudo tee -a /etc/pam.d/password-auth << 'EOF'
auth required pam_listfile.so item=user sense=deny file=/etc/ssh/denied_users onerr=succeed
EOF

   58  echo 'Hello from Student: 371205' | sudo tee /data/mephi-web/index.html
   59  sudo chown mephi-admin:mephi-devs /data/mephi-web/index.html
   60  sudo restorecon -v /data/mephi-web/index.html
   61  curl http://localhost
   62  curl http://192.168.1.100
   63  ping -c 4 192.168.1.1 > ~/network_check.txt
   64  ping -c 4 8.8.8.8 >> ~/network_check.txt
   65  journalctl -u nginx --since "5 minutes ago" > ~/nginx_recent_logs.txt
   66  cp /etc/fstab ~/fstab.txt
   67  getenforce > ~/selinux_status.txt
   68  ls -Zd /data/mephi-web > ~/file_contexts.txt
   69  getcap /usr/sbin/tcpdump > ~/tcpdump_capabilities.txt
   70  stat /data/mephi-web > ~/permissions.txt
   71  id mephi-admin > ~/users_groups.txt
   72  getent group mephi-devs >> ~/users_groups.txt
   73  curl -s http://192.168.1.100 > ~/curl_output.txt
   74  cp /data/mephi-web/index.html ~/
   75  sudo dnf download --destdir=/tmp tcpdump -y
   76  cp /tmp/tcpdump*.rpm ~/
   77  ls ~/*.txt ~/index.html ~/tcpdump*.rpm
   78  history > ~/project_history.txt
